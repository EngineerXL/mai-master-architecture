include:
  - "../docker-compose.yml"

services:
  user-service:
    tty: true
    image: user-service
    container_name: user-service
    cap_add:
      - SYS_PTRACE
    build:
      context: .
      dockerfile: user-service.dockerfile
      args:
        - BUILD_TYPE=${BUILD_TYPE}
